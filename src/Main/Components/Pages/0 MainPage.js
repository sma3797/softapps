import React from "react";
import classes from "./0 MainPage.module.css";

const MainPage = (props) => {
    return (
        <div className={classes.MainPage}>
            <div>MainPage</div>
        </div>
    );
};

export default MainPage;
